spring:
  application:
    name: DepEd_DB
  datasource:
    url: jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require
    username: postgres.snczzvgispwfkcfnwqev
    password: annedula.26@
    driver-class-name: org.postgresql.Driver

    # url: jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?sslmode=require
    # username: postgres.rgokduylkenylibgizpy
    # password: simonxpearl0303
    # driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

server:
port: ${PORT:5432}
